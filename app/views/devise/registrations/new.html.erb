<div class="main">
  <div class="inner">
    <div class="form__wrapper">
      <h2 class="page-heading">ユーザー新規登録</h2>

        <%# 「モデル名」にはUserモデルであれば@userを渡しましょう。%>
        <%# 「新規登録機能へのパス」は、devise導入後にrails routesを実行してdevise/registrations#createへのパスを確認し、記載してください。 %>
        <%= form_with model: @user, url: user_registration_path, local: true do |f| %>


        <div class="field">
          <%= f.label :email, "メールアドレス" %><br />
          <%= f.email_field :email, id:"user_email", autofocus: true, autocomplete: "email" %>
        </div>

        <div class="field">
          <%= f.label :password, "パスワード（6文字以上）" %><br />
          <%= f.password_field :password, id:"user_password", autocomplete: "new-password" %>
        </div>

        <div class="field">
          <%= f.label :password_confirmation, "パスワード再入力" %><br />
          <%= f.password_field :password_confirmation, id:"user_password_confirmation", autocomplete: "new-password" %>
        </div>

        <div class="field">
          <%= f.label :name, "店名" %><br />
          <%= f.text_field :name, id:"user_name" %>
        </div>

        <div class="field">
          <%= f.label :phone_number, "お店の電話番号" %><br />
          <%= f.text_area :phone_number, class: :form__text, id:"user_e_mail" %>
        </div>

        <div class="field">
          <%= f.label :access, "住所" %><br />
          <%= f.text_area :access, class: :form__text, id:"user_access" %>
        </div>

        <div class="field">
          <%= f.label :holiday, "定休日" %><br />
          <%= f.text_area :holiday, class: :form__text, id:"user_holiday" %>
        </div>

        <div class="field">
          <%= f.label :money, "お支払い手段（paypay,LINEpay,現金等 全角記号は使えません）" %><br />
          <%= f.text_area :money, class: :form__text, id:"user_money" %>
        </div>

        <div class="field">
          <%= f.label :word, "店主からの一言" %><br />
          <%= f.text_area :word, class: :form__text, id:"user_word" %>
        </div>

        <div class="field">
         <%= f.label :image, "お店の画像" %><br />
         <%= f.file_field :image, id:"user_image" %>
        </div>

        <div class="actions">
          <%= f.submit "新規登録", class: :form__btn  %>
        </div>
      <% end %>
    </div>
  </div>
</div>